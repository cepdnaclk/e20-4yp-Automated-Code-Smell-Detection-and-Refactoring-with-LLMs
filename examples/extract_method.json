{
  "smell": "Long Method",
  "strategy": "Extract Method",
  "before": "def process(data):\n    total = 0\n    for i in data:\n        if i > 0:\n            total += i\n    return total",
  "after": "def process(data):\n    return sum_positive(data)\n\n\ndef sum_positive(data):\n    total = 0\n    for i in data:\n        if i > 0:\n            total += i\n    return total"
}
